spring:
  ai:
    mcp:
      server:
        name: qiu-image-search-mcp-server
        version: 0.0.1
        type: SYNC
        stdio: true
  main:
    web-application-type: none
    banner-mode: off

# 关闭控制台日志输出，避免干扰 MCP stdio JSON-RPC 协议
logging:
  pattern:
    console: ""
